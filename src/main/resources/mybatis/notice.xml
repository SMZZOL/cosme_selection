<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.notice.NoticeDAOInter">

  <!-- 등록한 레코드 갯수 1 리턴 -->
  <insert id="create" parameterType="dev.mvc.notice.NoticeVO">
    INSERT INTO notice(noticeno, masterno, ntitle, ncontent, passwd, rdate)
    VALUES(notice_seq.nextval, #{masterno}, #{ntitle}, #{ncontent}, #{passwd}, sysdate)
  
     </insert>
     
  <!-- 모든 공지사항 목록 -->
  <select id="list_all" resultType="dev.mvc.notice.NoticeVO">
    SELECT noticeno, masterno, ntitle, ncontent, passwd, rdate
    FROM notice
    ORDER BY noticeno ASC
  </select> 
     
  <!-- 공지사항 조회 -->
  <select id="read" resultType="dev.mvc.notice.NoticeVO" parameterType="int">
    SELECT noticeno, masterno, ntitle, ncontent, passwd, rdate
    FROM notice
    WHERE noticeno = #{noticeno}
  </select> 
  
    </mapper>